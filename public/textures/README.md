# Generated texture assets

Place AI-generated transparent PNG vehicle decals in this directory using the structure documented in `docs/IMAGE_PROMPTS.md`.

The application currently falls back to procedural Canvas textures, so missing image assets never prevent the showroom from loading.
